#   ____  ____   _____
#  / __ \|  _ \ / ____|
# | |  | | |_) | |     ___  _ __ ___
# | |  | |  _ <| |    / _ \| '__/ _ \
# | |__| | |_) | |___| (_) | | |  __/
#  \___\_\____/ \_____\___/|_|  \___|
add_ace group.admin oxoadmin.menu allow
add_ace group.admin oxoadmin.self allow
add_ace group.admin oxoadmin.player allow
add_ace group.admin oxoadmin.server allow
add_ace group.admin oxoadmin.vehicle allow
add_ace group.admin oxoadmin.zones allow
add_ace group.admin oxoadmin.bans allow
add_ace group.admin oxoadmin.dev allow
add_ace group.superadmin oxoadmin.console allow
add_ace group.superadmin oxoadmin.code allow
add_ace group.admin oxoadmin.roles allow
## FiveM Documentation
## https://aka.cfx.re/server-commands

## QBCore Documentation
## https://docs.qbcore.org

## You CAN edit the following:
#
# --- Host panel (browser / txAdmin — NOT configured by endpoint_add — ask provider if URL fails) ---
# Given by host: http://135.148.136.32:5214 — also try https://135.148.136.32:5214
# If you see ERR_CONNECTION_REFUSED: service may be down, firewall, or port typo (e.g. 50214 vs 5214). Editing this file does not start the panel.
#
# --- Game server (FiveM client connect / FXServer listen — must match host "game server port" + port forwarding) ---
# Test: using port 5214 to match the number your host listed. If FXServer won't start ("address already in use") or players cannot connect, revert to 29668 below.
endpoint_add_tcp "0.0.0.0:50334"
endpoint_add_udp "0.0.0.0:50334"
# endpoint_add_tcp "0.0.0.0:29668"
# endpoint_add_udp "0.0.0.0:29668"
sv_maxclients 48
# OneSync — turn on here — qbx_core / ox_lib expect it. If FXServer errors, your host may require enabling OneSync in their panel instead (or a higher slot tier).
## [txAdmin CFG validator]: onesync MUST only be set in the txAdmin settings page.
# set onesync on
set steam_webApiKey none
# Increase vehicle durability (lower multiplier = less damage)
setr vMenu_vehicleDamageMultiplier 0.5
sets tags "dracula, nopixel4, illegal, qbcore"

## You MAY edit the following:
sv_licenseKey "cfxk_1J1J2AwrgCrYJnUOC4Ifx_4ZbEb6"
# Server name (shows in FiveM server list / browser)
sv_hostname "PHANTOM WORLD"
# Project title and short description (used in some in-game/server UIs)
sets sv_projectName "Phantom World"
sets sv_projectDesc "Enjoy a new city — join the discord discord.gg/phantomworld"
# Public listing: https://cfx.re/join/3m87mo (CFX_SERVER_ID for discord-bot /status)
sets locale "en-US"
load_server_icon myLogo.png
set sv_enforceGameBuild 3095
# MySQL — inline (semicolon form — @ in password is OK here)
set mysql_connection_string "host=eu.sql.gravelhost.com;user=u36165_iw7y32Vabb;password=.w7MljU.w5P@kAVirNr0MzgJ;database=s36165_9903ef012b;port=3306;charset=utf8mb4"
# If you still get Error 1045 after restart: the password is wrong in the panel, OR Gravelhost "Remote MySQL"
# does not allow your game server IP (console shows e.g. ns1009222.ip-135-148-136.us). In the panel add Remote
# Access / whitelist for % or that server IP, then save. Do not commit mysql.cfg to public repos.
#
# Local PC only (same machine as MySQL): comment the line above and copy the set line from mysql.cfg.example or docs
# One-time DB: run `resources/npwd/import.sql` in HeidiSQL / MySQL Workbench (creates npwd_* tables). QBX `players` needs `phone_number` (qbx_core / qbx_npwd).
# `vehicle_financing` for qbx_vehicleshop is auto-created on resource start — you can still run `resources/qbx_vehicleshop/vehshop.sql` manually if needed.

# Chat (merged from server.cfg.23)
set resources_useSystemChat true
set qbx_chat:joinMessage "^2%s joined the server"
set qbx_chat:quitMessage "^1%s left the server (%s)"

# Voice — full pma-voice options in voice.cfg (Dracula overrides at end of that file)
exec voice.cfg

sets Discord "https://discord.gg/phantomworld"
sets developed_by "Dracula"
# QBCore locale config
setr qb_locale "en"
# QBX: qb-core compatibility bridge (GetCoreObject, QBCore callbacks, DrawText, etc.)
setr qbx:enablebridge true
# Silence Qbox "Welcome to Qbox!" banner in console (optional)
setr qbx:acknowledge true

# Qbox extended (merged from server.cfg.23)
set qbx:enableQueue "true"
set qbx:bucketLockdownMode "inactive"
set qbx:discordLink "discord.gg/phantomworld"
set qbx:max_jobs_per_player 1
set qbx:max_gangs_per_player 1
set qbx:setjob_replaces "true"
set qbx:setgang_replaces "true"
set qbx:cleanPlayerGroups "true"
set qbx:allowMethodOverrides "true"
set qbx:disableOverrideWarning "false"
setr qbx:enableVehiclePersistence "false"
setr qbx:vehiclePersistenceType "semi"
setr qbx:enableGroupManagement "false"

# In-chat MOTD HTML (from server.cfg.23 — Phantom branding — edit or remove if you do not want chat HTML)
setr qbx:motd "<p style='padding: 1rem; background-color: #141517; border: 1px solid #373a40; border-radius: 0.5rem; text-align: center;'><span style='color: rgb(200, 200, 40);'>Welcome to Phantom World!</span><br>Discord: <span id='link' style='color: rgb(100, 100, 255);' onclick='window.invokeNative(\"openUrl\", \"https://discord.gg/phantomworld\")'>discord.gg/phantomworld</span></p><style>span#link:hover { cursor: pointer; }</style>"

# Renewed-Weathersync / CD disable (from server.cfg.23 — dr-weathersync is primary weather script)
setr weather_disablecd true

# Ox lib / ox_target / ox_inventory defaults — Phantom-specific overrides immediately below
exec ox.cfg

# NPWD — https://projecterror.dev/docs/npwd/start/installation/
# `standalone` avoids double PlayerLoaded (NPWD qbx bridge + qbx_npwd both registering). qbx_npwd handles Qbox integration.
set npwd:framework "standalone"
# Camera (Fivemanage / Imgur / Discord): see NPWD docs → Setting up Camera Functionality. Leave empty until you add a token.
set SCREENSHOT_BASIC_TOKEN ""
# Voice messages: pair with NPWD_AUDIO_TOKEN + voiceMessage in resources/npwd/config.json
set NPWD_AUDIO_TOKEN ""
# Optional: log reported tweets/marketplace to Discord (use set, not setr)
# set NPWD_DISCORD_TOKEN "https://discord.com/api/webhooks/..."

# loadscreen — NC Loading Screen V4 + renzu_spawn. true = loading screen stays until spawn UI calls ShutdownLoadingScreen
setr loadscreen:externalShutdown true
set loadscreen:primaryColor "#f1e542"
set loadscreen:shadowColor "#1a18077f"

# Targeting stack (ox only) — see targeting.cfg
setr dr-appearance:locale "en"
# illenium-appearance (same locale key as server.cfg.23 / Qbox template)
setr illenium-appearance:locale "en"
# ox_inventory config (keep backend only, disable its UI keybinds)
set inventory:framework "qbx"
set inventory:keys "[]"
set inventory:enablekeys "[]"
set inventory:enablestealcommand 0

# qbx_hud top strip (rank / job lines)
setr qbx_hud:showRank 1
setr qbx_hud:showJob 1

# These resources will start by default.
ensure mapmanager
# spawnmanager: qbx_core still uses exports.spawnmanager (setAutoSpawn false + spawnPlayer for fallbacks). Do NOT stop it unless you patch qbx_core.
ensure spawnmanager
ensure sessionmanager
# basic-gamemode gametype must run — default maps bind to it — if stopped, session/load can hang (black loadscreen). Stock script is patched: setAutoSpawn(false) only, no forceRespawn (qbx_core owns spawn).
ensure basic-gamemode
# Freeroam map + spawnpoint definitions — before players connect so spawnmanager has points when onClientMapStart fires
# Folder missing on this host (no resources/[defaultmaps]) — re-enable when populated.
# ensure [defaultmaps]
# hardcap off — conflicts with qbx_core built-in queue (qbx:enableQueue) — it stops hardcap when queue runs
# ensure hardcap
ensure baseevents
# Default Cfx chat (qbx_core OnPlayerLoaded uses exports.chat:addMessage for MOTD — admin menu uses chat:addSuggestion)
ensure chat
# Nodecraft Pro — `resources/[nodecraft]/playerlist` (panel connection logs). Allowlist OFF so anyone can join.
setr nc.enableAllowlist false
# Folder missing on this host (no resources/[nodecraft]) — re-enable when populated.
# ensure [nodecraft]
# ensure [gameplay]    # disabled for QBX-native boot (many qb-core scripts inside)

# Core framework & extra stuff
# Now running Qbox (`qbx_core`) instead of legacy QBCore.
# qbx_core registers QB-compat exports on the `qb-core` name (see bridge `__cfx_export_qb-core_*`).
# The old core folder was renamed to `qb-core-legacy` so it does not steal the `qb-core` resource name
# (a stopped real `qb-core` resource made `exports['qb-core']:GetCoreObject()` fail for all scripts).
# ensure qb-core-legacy          # reference only — do not start alongside qbx_core
ensure ox_lib
ensure PolyZone
ensure oxmysql
# Do NOT `ensure [qb]`: it starts qb-inventory, qb-clothing, qb-banking, apartments, etc., then this cfg
# stops many of them. Scripts that query MySQL on start then get stopped → stale oxmysql Lua refs → nested ref errors.
ensure qbx_core
exec targeting.cfg
# ox_inventory crafting/shops call ox_target exports — must start after ox_target (targeting.cfg).
ensure ox_inventory
exec needs.cfg
# Belt-and-suspenders if something auto-starts these — harmless when they were never started.
stop qb-core-legacy
stop qb-inventory
stop qb-cityhall
stop qb-weathersync
stop qb-vehiclekeys
stop qb-garages
stop qb-vehicleshop
# Stop all weather scripts to prevent conflicts with dr-weathersync
stop Renewed-Weathersync
# stop Renewed-Banking  # resource not installed
# Explicit `[qb]` scripts to run (everything in the folder minus conflicting/disabled packs — see resources/[qb]/).
ensure qb-smallresources
ensure qb-interior
ensure qb-weapons
ensure qb-prison
ensure qb-management
ensure qb-drugs
# qb-pawnshop off — use qbx_pawnshop later (avoid two pawn systems)
ensure qb-minigames
ensure progressbar
ensure mz-skills
ensure qb-doorlock
ensure qb-doorlock-main
ensure qb-radargun
ensure qb-drones
ensure qb-securityjob
ensure qb-menu
ensure qb-input
ensure qb-trucker
ensure qb-garbagejob
ensure qb-towjob
ensure qb-delivery
# Shared QBCore resolver for dr-* scripts (must load before resources that depend on it).
ensure dr-qbx-compat
# Character / skin UI (ox_lib + qbx_core above — see docs/SPAWN_APPEARANCE_ORDER.md — run illenium SQL if needed).
ensure illenium-appearance
# Phantom Core - Custom UI, Vehicle, Player, Economy, and Mechanics
ensure phantom_core
# Phantom Heists - Banks, Jewelry & Armored Trucks
ensure phantom_heists
# Phantom Gangs - Territory Wars & Red Zones
ensure phantom_gangs
# Phantom Cops vs Robbers - PvP Arena System
ensure phantom_copsvrobbers
# Phantom Events - Admin Events, Money Drops, Free Cars
ensure phantom_events
# Phantom Garage - GTA Online Style Garages
ensure phantom_garage
# New custom scripts
ensure jg-dealerships-v2
# Mechanic shops: kaves_mechanic + phantom_mechanic (job `mechanic` players only — see mechanic.cfg)
exec mechanic.cfg
# Nested under [custom]/inside-interaction/inside-interaction — flatten or keep ensure after fix.
ensure inside-interaction
ensure pw-rankgating
ensure Afterlife_inventoryradial
ensure electus_bodyguards
# One-time MySQL: run resources/[custom]/electus_bodyguards/electus_bodyguards.sql in HeidiSQL/phpMyAdmin
ensure [pillboxhospital]
stop kvl-truck
# Phantom Weapons - GTA Online Weapon Wheel & Ammu-Nation
ensure phantom_weapons
# Next Death - Advanced death/revive system
ensure next_death
ensure fenix-ems
# Next Housing - Complete housing system (replaces qb-houses)
ensure next_housing
# ND Resources (Andyyy) - QBX-bridged police/ambulance/fuel/MDT/banking/appearance
ensure ND_Core
stop ND_Ambulance
ensure ND_Police-main
ensure ND_Fuel-main
ensure ND_MDT
ensure ND_Banking
ensure ND_AppearanceShops
# GTA-Online Style Weapon Wheel (standalone, works alongside phantom_weapons)
ensure gta_weapon_wheel
# Vehicle Calling Menu - spawn owned/rented/job vehicles
ensure vehicle_caller
# Multichar + spawn: Afterlife IV Multicharacter system
ensure Afterlife_ivmulticharacter2.0
# renzu systems stopped - using Afterlife multichar instead
# stop renzu_multicharacter  # resource not installed
# stop renzu_spawn  # resource not installed
# stop qbx_spawn  # resource not installed
stop qbx_apartments
stop qbx_interior
# Single clothing stack — illenium replaces qb-clothing (same DB table names — illenium owns `player_outfits` schema).
stop qb-clothing
ensure qbx_vehicles
# QBX City Hall + ID cards (uses `qbx_cityhall-main` + `qbx_idcard` — legacy `qb-cityhall` stays off — it expects qb-inventory)
ensure qbx_idcard
ensure qbx_cityhall-main
# Dependency required by qbx_idcard
ensure MugShotBase64
# disable default ps-hud and qbx_hud - dr-hud is now the primary HUD (see line ~281)
stop ps-hud
stop qbx_hud
ensure XNLRankBar
ensure dr-starterpack
# renzu_hud disabled (using dr-hud) - was causing oxmysql orphan callback warnings
# ensure renzu_hud
ensure dr-admincar
stop dr-admin
# ensure 0r_lib        # disabled (old 0r_houses only)
ensure interact
ensure object_gizmo
ensure ox_doorlock
ensure fivem-freecam
# Stopping ps-housing - next_housing is the active housing system
stop ps-housing-2.0.7
# qb-houses replaced by next_housing
stop qb-houses
# Built-in Qbox multichar does not use renzu_* (stays stopped if something auto-starts them).
ensure dr-rankxp
# cdn-fuel replaced by ND_Fuel-main
ensure interact-sound
stop cdn-fuel
# Qbox: keys + garages + vehicle shop (see resources/qbx_*)
ensure qbx_vehiclekeys
stop qbx_vehicleshop
# Vehicle customization — mechanic.cfg (kaves — renzu stopped)
# Qbox scripts that live under `resources/[qbx]/` (same as `ensure [qbx]` — listed explicitly: some hosts/containers
# fail to start bracket-named resource groups, or SFTP mangles `[qbx]`). Order: qbx_medical before qbx_ambulancejob.
# next_death replaces qbx_medical + qbx_ambulancejob + ND_Ambulance
stop qbx_medical
stop qbx_ambulancejob
stop qb-ambulancejob
ensure qbx_chat_theme
# Multichar (renzu_spawn) + apartments disabled (renzu handles its own housing). Spawn UI is renzu_spawn only.
ensure dr-weathersync
# stop qbx_spawn  # resource not installed
stop qbx_apartments
stop qbx_interior
ensure qbx_garages
ensure qbx_houserobbery
ensure qbx_pawnshop
stop qbx_streetraces -- Resource directory doesn't exist
# qbx_hud ensured above with dr-hud stopped
# qb-menu, qb-input, jobs in `[qb]` are ensured with qbx_core above (avoid ensure [qb]).
ensure xt-robnpcs
ensure jobs_creator
# OMES Banking (in-building + banker peds) – stop qb-banking to avoid double banking at same spots
stop qb-banking
# stop renewed-banking  # resource not installed
# stop Renewed-Banking  # resource not installed
ensure omes_banking
# Resource groups: txAdmin shows "Start" until listed here.
ensure [standalone]
stop cdn-fuel
ensure [tuff]
# renzu_hud disabled - reverted to dr-hud
# stop renzu_hud  # resource not installed
ensure Rup-Fishing
ensure sm_hunting
ensure car_exporter
ensure admin_auto_keys
ensure norse_awsome_admins
# Do NOT auto-start all `[New]` resources: it starts legacy spawn/multichar/elevator scripts before stop lines run.
# Start only explicit `ensure dr-*` resources below to prevent QBX spawn/weather conflicts.
# ensure [New]
# Qbox spawn flow guard: these old spawn/intro resources can lock players on black/loading screen.
stop dr-firstspawn
stop dr-multicharacter
stop connectqueue
# Using tuff-hud as primary HUD (dr-hud disabled for tuff-hud)
stop dr-hud
ensure tuff-hud
# Tuff Scoreboard - custom scoreboard
ensure tuff-scoreboard
# Tuff Pause Menu - v0.0.2 (updated, root resources/tuff_pausemenu takes precedence)
stop tuff_pausemenu
ensure tuff_pausemenu
# Stop dr-admin (user request - in the way)
stop dr-admin
# Rup-Fishing replaces dr-fishing
stop dr-fishing
# Using dr-animationmenu instead of scully_emotemenu
ensure dr-animationmenu
stop scully_emotemenu
# qb-policejob and qbx_police replaced by ND_Police-main
stop qb-policejob
stop qbx_police
# ps-mdt replaced by ND_MDT — ps_lib kept for other resources
ensure ps_lib
stop ps-mdt
# Stop ugly qb-deathscreen - use default or dr alternative
stop qb-deathscreen
# Phantom loadscreen - using NC Loading Screen V4
stop loadingscreen
# stop dr-loadingscreen  # resource not installed
stop qs-loadingscreen-master
stop tuff-loading
# nc-loadingscreenv-main is ensured at end of cfg

ensure [heists]
# New QBCore robbery systems (downloaded)
ensure qb-bankrobbery
# Stub/empty qb-houserobbery — real resource is qbx_houserobbery (ensured above).
# ensure qb-houserobbery
# Old duplicate resources removed from resources/ — see _archive/ (not scanned as resources).
# qb-apartments stopped — multichar/spawn is Qbox built-in (`resources/[Newmaps]/[qb]/qbx_core` config).
stop qb-apartments
# qb-weathersync removed – dr-weathersync is the only weather script
ensure y_burgershot-main
# lusty94_shops merged into ox_inventory `data/shops.lua` (single shop system). Re-enable only if you add missing items (weed, veh_*, heist tools) to ox_inventory.
stop lusty94_shops
# ensure separate-mode  # disabled for QBX-native boot (contains many qb-core scripts)
ensure dr-ownerlabel
ensure dr-ownervest
ensure [voice]
# NPWD + qbx_npwd — pma-voice from `ensure [voice]` above — screenshot-basic must run before npwd.
ensure screenshot-basic
ensure npwd
ensure qbx_npwd
ensure payphones
# Phone: legacy qb-phone off — use NPWD + qbx_npwd
stop qb-phone
# qbx_phone requires qbx_apartments — use NPWD instead unless you add apartments.
stop qbx_phone
# [defaultmaps] ensured with mapmanager/spawnmanager/basic-gamemode (patched — see top of file)
# Categories below: add resource subfolders (each with fxmanifest.lua) so they start.
# [standalone] + [heists] are ensured earlier in this file. [scripts]/[multiplayer]/[Newmaps] add here when populated.
# See resources/WHERE_TO_PUT_RESOURCES.txt for where to put new resources.
# ensure [scripts]
# ensure [multiplayer]
# ensure [interactions]
# ensure [minigames]
# Folder missing on this host — re-enable when populated.
# ensure [Graphics]
# vMenu: set permissions + options BEFORE starting vMenu
# (Inline the active parts of `resources/vMenu/config/permissions.cfg` so we don't
# spam the console with "No such command ..." errors during `exec`.)
setr vmenu_use_permissions true
setr vmenu_menu_staff_only false
setr vmenu_disable_player_stats_setup false
setr vmenu_menu_toggle_key "F5"
setr vmenu_noclip_toggle_key "F2"
setr vmenu_keymapping_id "Default"
setr vmenu_keep_spawned_vehicles_persistent false
setr vmenu_auto_ban_cheaters false
setr vmenu_auto_ban_cheaters_ban_message "You have been automatically banned. If you believe this was done by error, please contact the server owner for support."
setr vmenu_log_ban_actions true
setr vmenu_log_kick_actions true
setr vmenu_use_els_compatibility_mode false
setr vmenu_handle_invisibility true
setr vmenu_quit_session_in_rockstar_editor false
setr vmenu_server_info_message "About this server, discord: vespura.com/discord"
setr vmenu_server_info_website_url "www.vespura.com"
setr vmenu_teleport_to_wp_keybind_key 168
setr vmenu_disable_spawning_as_default_character true
setr vmenu_enable_animals_spawn_menu false
setr vmenu_pvp_mode 0
setr keep_player_head_props true
setr vmenu_player_names_distance 500.0
setr vmenu_disable_entity_outlines_tool false
setr vmenu_default_ban_message_information "Please contact the staff team by going to (support url) if you want to appeal this ban"
setr vmenu_enable_weather_sync false
setr vmenu_enable_dynamic_weather false
setr vmenu_dynamic_weather_timer 15
setr vmenu_current_weather "clear"
setr vmenu_blackout_enabled false
setr vmenu_weather_change_duration 30
setr vmenu_enable_snow false
setr vmenu_enable_time_sync false
setr vmenu_freeze_time false
setr vmenu_ingame_minute_duration 2000
setr vmenu_current_hour 7
setr vmenu_current_minute 0
setr vmenu_sync_to_machine_time false
setr vmenu_using_chameleon_colours false
setr vmenu_prevent_extras_when_damaged false
setr vmenu_allowed_engine_damage_for_extra_change 800
setr vmenu_allowed_body_damage_for_extra_change 800
setr vmenu_mp_ped_preview true
setr vmenu_override_voicechat_default_range 0.0

# vMenu ACL (if txAdmin logs "Access denied for command add_ace", enable cfg execution for ACE in txAdmin or paste these into the server CFG it runs with full rights)
add_ace group.admin "vMenu.DontKickMe" allow
add_ace group.admin "vMenu.DontBanMe" allow
add_ace group.admin "vMenu.Everything" allow
add_ace group.admin "vMenu.Staff" allow
# vMenu — owner/admin only — moderators and players cannot open it
#add_ace group.moderator "vMenu.Staff" allow
#add_ace builtin.everyone "vMenu.Staff" allow

add_principal group.admin group.moderator

add_ace group.moderator "vMenu.OnlinePlayers.Summon" allow
add_ace group.moderator "vMenu.OnlinePlayers.Kill" allow
add_ace group.moderator "vMenu.OnlinePlayers.Kick" allow
add_ace group.moderator "vMenu.OnlinePlayers.TempBan" allow
add_ace group.moderator "vMenu.OnlinePlayers.ViewBannedPlayers" allow

add_ace group.admin "vMenu.OnlinePlayers.Identifiers" allow
add_ace group.admin "vMenu.OnlinePlayers.PermBan" allow
add_ace group.admin "vMenu.OnlinePlayers.Unban" allow

# Weather permissions - ONLY admins can change weather (non-admins denied)
add_ace group.admin "vMenu.Weather.ChangeWeather" allow
add_ace group.admin "vMenu.Weather.DynamicWeather" allow
add_ace group.admin "vMenu.Weather.SetTime" allow
add_ace group.admin "vMenu.Weather.FreezeTime" allow
add_ace group.admin "vMenu.MiscSettings.TeleportSaveLocation" allow
add_ace group.admin "vMenu.MiscSettings.EntitySpawner" allow
add_ace group.developer "vMenu.MiscSettings.DevTools" allow
add_ace group.moderator "vMenu.VoiceChat.StaffChannel" allow

# Deny weather permissions to non-admin groups
add_ace group.moderator "vMenu.Weather.ChangeWeather" deny
add_ace group.moderator "vMenu.Weather.DynamicWeather" deny
add_ace group.moderator "vMenu.Weather.SetTime" deny
add_ace group.moderator "vMenu.Weather.FreezeTime" deny
add_ace builtin.everyone "vMenu.Weather.ChangeWeather" deny
add_ace builtin.everyone "vMenu.Weather.DynamicWeather" deny
add_ace builtin.everyone "vMenu.Weather.SetTime" deny
add_ace builtin.everyone "vMenu.Weather.FreezeTime" deny

# Addon vehicles — start before vMenu so cars2 streams before the spawner menu
# Folders missing on this host ([vehicles], cars2) — re-enable when packs are uploaded.
# ensure [vehicles]
# Addon engine sounds (SpiritsCreations pack in resources/Audio_Pack).
# Disabled: current pack is incomplete (missing most .dat/.awc files), causing mass client load failures.
# Re-enable only after uploading the full pack files.
# ensure Audio_Pack
# ensure cars2
# vMenu folder: resources/vMenu — menu toggle F5 (string key name — NPWD uses F4 for phone). Spawner uses config/addons.json (matches cars2).
ensure vMenu
ensure phantom_vehicle_balance
# [New] scripts: `ensure [New]` above (plus explicit `ensure dr-*` lines below where needed).
# Multichar: Qbox built-in (qbx_core). dr-multicharacter + dr-spawnselector stay stopped — they conflict with qbx_core flow.
stop dr-multicharacter
stop dr-spawnselector
stop dr-firstspawn
stop dr-elevator

# Extra loadscreen packs (keep stopped so only `qs-loadingscreen-master` is active)
stop ls_loadingscreen-main

ensure dr-mapblips
ensure dr-serverinfo
ensure aihelp
ensure dr-membership
stop dr-storerobbery
# New Scripts - Robbery Systems
ensure qb-bankrobbery
ensure qb-houses
ensure mz-storerobbery
stop hybrid-storerobbery
stop lation_247robbery
stop ps-mdt
# Enhanced store robbery system (QBCore)
# ps-ui minigames now included in ps_lib (Thermite, Scrambler, etc.)
# Optional: mz-skills (already added above) for skill progression
ensure dr_world_events
# AI Police (GTA-style wanted/dispatch, hooked into store/bank robberies)
ensure fenix-police
ensure dr-mainmenu
ensure OT_pushvehicle
# [vehicles] + cars2 are ensured above (before vMenu)
# FiveM Vehicle Pack: `vehicles.cfg` uses `start 1`, `start 2`, … — those resources must exist as `resources/1`, etc.
# Uncomment when your pack is laid out that way (see pack README). The container resource is ensured below.
# Clothing packs (EUP etc.). If you add illenium-appearance as its own resource, keep it near ox_lib/qbx_core above, not only here.
# Folders missing on this host — re-enable when packs are uploaded.
# ensure [clothing]
# ensure [gangs]
# ensure [Newmaps]

# New scripts added to resources folder (QBCore-compatible where applicable)
ensure mnr_cayo
ensure patoche_bridge
ensure UpgradedSandy
ensure "Fishnet Socks cDev"
# Solara outfit XMLs may exist as files — do NOT ensure a Solara resource (known unwanted menu).
# ensure Solara
# stop Solara  # resource not installed
# Pack not present on this host — re-enable when uploaded.
# ensure AzuraPlatV2-Branded
# Azura clothing pack: console may show hundreds of "Oversized assets" warnings (64MB textures). Expected — causes streaming hitches, not script errors.
# Root resources not covered by `ensure [group]` lines above
ensure dr-aihelp
ensure dr-vehdamage
ensure Fishnet
ensure player-blips
ensure notification-center
# Previously idle [New] resources - now enabled per user request
ensure dr-Drivingschool
ensure dr-Earthquake
ensure dr-ammorobbery
ensure dr-bodycam
ensure dr-bodycamprop
ensure dr-carcontrol
ensure dr-fleeca
ensure dr-ganggarage
ensure dr-mosleys
stop dr-playerlist
ensure dr-radialmenu
ensure dr-realoffroad
ensure dr-rental
ensure dr-roofrunning
ensure aihelp
ensure ox_cayobar
ensure ox_cayobar-main
ensure server
ensure cfx-server-data-master
# "FiveM Vehicle Pack" is optional (minimal fxmanifest + optional vehicles.cfg). If FXServer prints "Couldn't find resource",
# the folder is missing from this machine's `resources/` path, or rename to e.g. FiveM-Vehicle-Pack and match `ensure` exactly.
# Uncomment when the folder exists and you want it loaded:
# ensure "FiveM Vehicle Pack"
# exec "resources/FiveM Vehicle Pack/vehicles.cfg"
#
# Custom scripts - ensure admin menu first
ensure dr-mainmenu
ensure dr-wanted

# EasyAdmin (staff menu — dr-mainmenu Admin button runs `easyadmin`)
ensure EasyAdmin

## Permissions ##
# Base Qbox permissions (merged from server.cfg.23 — also satisfies vMenu note about permissions.cfg)
exec permissions.cfg

add_ace group.admin admin allow
add_ace group.admin easyadmin allow
add_principal identifier.fivem:13816790 group.admin
add_principal identifier.discord:769964740008083526 group.admin
# Add your FiveM ID below (replace 00000000 with your ID - see server console when you join)
add_principal identifier.fivem:14311541 group.admin
# Owner license (added by Cascade)
add_principal identifier.license:5d42954dcf1e1e2444564e7f456d89ebc6c8ca76 group.admin
add_principal identifier.license2:5d42954dcf1e1e2444564e7f456d89ebc6c8ca76 group.admin

# Resources
# add_ace resource.qb-core command allow   # OLD: qb-core
add_ace resource.qbx_core command allow

# Gods
add_ace qbcore.god command allow

# Inheritance
add_principal qbcore.god group.admin
add_principal qbcore.god qbcore.admin
add_principal qbcore.admin qbcore.mod

# Players
add_principal identifier.discord:769964740008083526 qbcore.god
add_principal identifier.discord:413173364216168449 qbcore.god
add_principal identifier.license:5d42954dcf1e1e2444564e7f456d89ebc6c8ca76 qbcore.god
add_principal identifier.license2:5d42954dcf1e1e2444564e7f456d89ebc6c8ca76 qbcore.god

# Optional server tuning (misc.cfg is mostly commented examples — one active line: busy spinner)
exec misc.cfg

# Late stops — txAdmin or another cfg may `ensure` these after earlier lines — OMES is your bank.
# stop renewed-banking  # resource not installed
# stop Renewed-Banking  # resource not installed
# Final weather script stops - ensure ONLY dr-weathersync controls weather
stop qb-weathersync
stop Renewed-Weathersync
# stop qbx-weathersync  # resource not installed
exec targeting.cfg
exec mechanic.cfg
exec needs.cfg

setr ox:primaryColor yellow
setr ox:primaryShade 4

# --- Keybind reference (all resolved conflicts) ---
# F1=serverinfo F3=emotes F4=scoreboard F5=vMenu F6=mechanic-tablet
# F7=citytour F8=scoreboard-hold F9=player-blips F10=mainmenu
# F11=voice-proximity F12=EasyAdmin HOME=Norse-admin
# E=proximity-interact G=engine L=lock M=phone T=cruise U=ragdoll
# LCTRL=crouch LALT=radio Y=answer-phone J=decline-phone

# --- Spawn stack (wins over earlier lines): renzu_spawn map UI + Afterlife IV Multicharacter — stop Qbox spawn hijacks ---
# stop qbx_spawn  # resource not installed
stop qbx_apartments
stop qbx_interior
stop dr-multicharacter
# stop renzu_multicharacter  # resource not installed
# stop qbx_spawn  # resource not installed
stop qbx_apartments
stop qbx_interior

# --- SPAWN STACK: Loading screen + Afterlife Multichar ---
# Stop all other loading screens first
stop loadingscreen
stop ls_loadingscreen-main
# stop qbx_loadingscreen  # resource not installed
# stop qbx-loading-screen  # resource not installed
stop qs-loadingscreen-master
stop tuff-loading

# NC Loading Screen — live stats (uptime from CFX listing, jobs from qbx_core)
set nc_loadscreen:cfxId "3m87mo"
set nc_loadscreen:vehicleCount "300"

# Start NC Loading Screen V4
ensure nc-loadingscreenv-main

# Afterlife multicharacter is ensured earlier in the file

# Phantom World City Tour - Premium cinematic tour system
ensure phantom_citytour
# Phantom dashboard Discord webhook + bot relay (secrets in phantom_dashboard.secrets.cfg)
exec phantom_dashboard.secrets.cfg
# Compact info strip (bottom-left) + Discord status HTTP bridge
ensure phantom_dashboard
ensure prime-vangelico
ensure vehiclehandler
ensure informational
ensure mrm-crosshair
ensure HRSGears
stop np-polyzone
stop polyzone-lib

# Deploy: full server.cfg synced from repo (do not remove lines above)
