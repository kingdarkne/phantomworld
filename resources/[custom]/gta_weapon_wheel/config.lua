-- Legacy config kept so old references do not error.
-- The NUI custom wheel is permanently disabled (caused mouse-lock freezes).
Config = {
    Enabled = false,
    OpenKey = 37,
    OpenKeyName = 'TAB',
    BlockNativeWheel = false,
    UseNativeGtaWheel = true,
}
