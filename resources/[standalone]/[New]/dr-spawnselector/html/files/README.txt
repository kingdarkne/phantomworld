Map image for spawn selector
============================

To get the full look from the script preview video, add a Los Santos map image here as:

  map.png

Use a top-down or isometric map image of the city (e.g. from the script's documentation or the developer). The UI will use it as the background; without it, a dark background is shown.
